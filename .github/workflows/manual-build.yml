name: Manual Build
run-name: Manual Build ${{ github.ref_name }}

on:
  workflow_dispatch:

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml
    with:
      commit_ref: ${{ github.sha }}
